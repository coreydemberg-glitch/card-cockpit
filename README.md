# Card Cockpit
COMC -> SlabSharks flip tracker. Two ways to run:
- **Static (GitHub Pages):** `index.html` — data saved in your browser (localStorage).
- **Server (Glitch/Railway/local):** `npm start` -> http://localhost:4141 — data persists in `data.json`, syncs across devices.
